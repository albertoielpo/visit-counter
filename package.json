{
    "name": "visit-counter",
    "version": "1.0.4",
    "description": "visit counter",
    "main": "dist/app.js",
    "scripts": {
        "start": "tsc && node dist/app.js",
        "start:debug": "tsc && node --inspect=0.0.0.0 --trace-warnings dist/app.js",
        "format": "prettier --write \"src/**/*.ts\"",
        "compile": "tsc",
        "build": "tsc",
        "audit:prod": "npm audit --omit=dev",
        "version:patch": "npm version patch --no-git-tag-version && sed -i \"s/APP_VERSION = \\\".*\\\"/APP_VERSION = \\\"$(node -p \"require('./package.json').version\")\\\"/\" src/common/config.ts && git add package.json package-lock.json src/common/config.ts && git commit -m \"Chore: $(node -p \"require('./package.json').version\")\"",
        "version:minor": "npm version minor --no-git-tag-version && sed -i \"s/APP_VERSION = \\\".*\\\"/APP_VERSION = \\\"$(node -p \"require('./package.json').version\")\\\"/\" src/common/config.ts && git add package.json package-lock.json src/common/config.ts && git commit -m \"Chore: $(node -p \"require('./package.json').version\")\"",
        "version:major": "npm version major --no-git-tag-version && sed -i \"s/APP_VERSION = \\\".*\\\"/APP_VERSION = \\\"$(node -p \"require('./package.json').version\")\\\"/\" src/common/config.ts && git add package.json package-lock.json src/common/config.ts && git commit -m \"Chore: $(node -p \"require('./package.json').version\")\""
    },
    "author": "Alberto Ielpo",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "~25.3.0",
        "prettier": "~3.8.1",
        "typescript": "~5.9.3"
    },
    "jest": {
        "moduleFileExtensions": [
            "js",
            "json",
            "ts"
        ],
        "rootDir": "src",
        "testRegex": ".*\\.spec\\.ts$",
        "testEnvironment": "node"
    },
    "engines": {
        "node": ">=24",
        "npm": ">=11"
    },
    "dependencies": {
        "@albertoielpo/ielpify": "~0.3.0",
        "@fastify/static": "~9.0.0",
        "@fastify/view": "^11.1.1",
        "fastify": "~5.7.4",
        "handlebars": "^4.7.8",
        "pino-pretty": "~13.1.3",
        "redis": "~5.11.0",
        "reflect-metadata": "~0.2.2"
    }
}
